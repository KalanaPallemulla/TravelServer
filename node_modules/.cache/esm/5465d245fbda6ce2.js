let express,formidable,Cart;_fbd‍.w("express",[["default",["express"],function(v){express=v}]]);_fbd‍.w("express-formidable",[["default",["formidable"],function(v){formidable=v}]]);_fbd‍.w("../models/Cart",[["default",["Cart"],function(v){Cart=v}]]);



const router = express.Router();

router.post("/addtocartmen", async (req, res) => {
  const data = req.body;
  try {
    let cart = new Cart(data);

    cart.save((err, result) => {
      if (err) {
        return res.status(400).send(err);
      }
      res.json(result);
    });
  } catch (error) {
    _fbd‍.g.console.log("error==>", error);
  }
});

router.get("/menCart", async (req, res) => {
  try {
    let cart = await Cart.find({}).exec();
    res.json(cart);
    // cart.items.map(async (service) => {
    //   let men = await Men.findById(service.service);
    //   res.json(service);
    // });
  } catch (err) {
    _fbd‍.g.console.log("error==>", err);
  }
});

// router.get("/menCartId/:id", async (req, res) => {
//   try {
//     let cart = await Cart.findById(req.params.id);

//     cart.items.map(async (service) => {
//       let men = await Men.findById(service.service);

//       res.json(cart + men);
//     });
//   } catch (err) {
//     console.log("error==>", err);
//   }
// });

module.exports = router;
