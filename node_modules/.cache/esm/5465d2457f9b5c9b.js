let express,formidable,Cart;_7f9‍.w("express",[["default",["express"],function(v){express=v}]]);_7f9‍.w("express-formidable",[["default",["formidable"],function(v){formidable=v}]]);_7f9‍.w("../models/Cart",[["default",["Cart"],function(v){Cart=v}]]);


const router = express.Router();

router.post("/cart", async (req, res) => {
  const data = req.body;
  _7f9‍.g.console.log(req.body);
  try {
    let cart = new Cart(data);

    cart.save((err, results) => {
      if (err) return res.status(400).send(err.message);
      return res.send(results);
    });
  } catch (err) {
    _7f9‍.g.console.log(err);
  }
});

router.get("/cart", async (req, res) => {
  try {
    let cart = await Cart.find().sort("-createdAt").exec();
    res.send(cart);
  } catch (err) {
    _7f9‍.g.console.log("error==>", err);
  }
});

module.exports = router;
